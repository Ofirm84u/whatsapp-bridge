# Server
PORT=3020
HOST=0.0.0.0

# Auth state directory (persists WhatsApp session)
AUTH_STATE_DIR=./auth_state

# Webhook URL — incoming text messages are POSTed here
# WEBHOOK_URL=http://localhost:8010/api/whatsapp/incoming

# Log level: debug | info | warn | error
LOG_LEVEL=info
